[package]
name = "memory"
edition = "2024"
license = "Apache-2.0"
authors = ["Lethe (Sui Overflow 2026)"]

# Sui, MoveStdlib and system packages are auto-resolved by the Sui CLI
# (1.72+) to match the environment you publish to (testnet). No explicit
# framework dependency needed — see `sui move build` notes.
[dependencies]

[addresses]
memory = "0x0"
