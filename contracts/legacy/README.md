# Legacy Move packages (deprecated)

These packages belong to **old pivots**, kept for reference only — not part of the
current aim (user-owned portable AI memory on Walrus/MemWal). See [REAIM.md](../../REAIM.md).

- `lethe/` — `artwork.move`, the AI-art NFT pivot.
- `battle/` — `battle.move`, the head-to-head voting pivot.

The active package is `contracts/memory/` (`memory::memory`). Do not build on these.
