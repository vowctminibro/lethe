# LEGACY (pre-pivot, kept for history)

This standalone memory service is from an earlier era and is not used by the current app.
The current app lives in [`apps/web`](../web) — see the root [README](../../README.md).
